tambah sungai
	selesai draw simpan ke state geodata > river 
	isi atribut simpan ke form state form > river & ke geodata > river

tambah project
	selesai draw simpan ke state geodata > project
	isi atribut simpan ke form state form > project & ke geodata > project

ambil data river & project
	makeSelectRiverData => this.props.riverData
	makeSelectProjectData => this.props.projectData

upload geodata river
	updateGeodataRiver (geojson)

yg smentara ada di script
setelah draw selesai dia panggil _onDrawCreate => this.props.insertRiverFeatures
proses ini menginsert array features ke geodata > river > features

casse INSERT_RIVER_FEATURES:
return state.updateIn(['geodata','river','features'], features => features.push(action.payload));

6194800000002